# v0.5.0-alpha.1 Release Notes (Draft)

## Scope
This alpha snapshot advances runtime-controls hardening and enterprise retrieval coverage.

## Highlights
- SharePoint read-only connector (Microsoft Graph) with policy-scoped retrieval.
- Webhook dead-letter replay CLI with deterministic idempotency and replay reporting.
- Benchmark trend regression checks against checked-in governance baseline.
- CI expansion for Redis runtime coverage and webhook replay smoke.

## Added
- Connector:
  - `app/rag/connectors/sharepoint.py`
- Scripts:
  - `scripts/replay_webhook_dead_letter.py`
  - `scripts/check_benchmark_trend.py`
- Baseline:
  - `benchmarks/baselines/governance_enforce_redact_summary.json`

## Changed
- Runtime settings:
  - `SRG_RAG_SHAREPOINT_BASE_URL`
  - `SRG_RAG_SHAREPOINT_SITE_ID`
  - `SRG_RAG_SHAREPOINT_DRIVE_ID`
  - `SRG_RAG_SHAREPOINT_BEARER_TOKEN`
  - `SRG_RAG_SHAREPOINT_ALLOWED_PATH_PREFIXES`
  - `SRG_RAG_SHAREPOINT_CACHE_TTL_SECONDS`
- CI workflow:
  - Redis service added to test job
  - webhook dead-letter replay smoke step
  - governance trend regression gate

## Verification
- Local gates:
  - `ruff` passes
  - `mypy` passes
  - `pytest` passes
- Integration behavior:
  - Redis budget backend accounting covered
  - OTLP HTTP exporter covered with local collector stub
  - SharePoint connector citation path covered

## Known Gaps Before Final v0.5.0
- Run and publish kind runtime-controls smoke outputs as weekly benchmark artifact.
- Cut prerelease tag and attach release evidence bundle.
