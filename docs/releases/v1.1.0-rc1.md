# v1.1.0-rc1 Release Notes

Release date: 2026-03-08

## Summary

`v1.1.0-rc1` is the release-candidate cut for the reliability/SLO hardening track.

This cut is hardening-only and keeps API/contracts stable while proving operational readiness.

## Highlights

1. Stabilization-window readiness:
- Confirmed 7-day window criteria for core operations workflows:
  - `deploy-smoke >= 3`
  - `release-verify >= 2`
  - `ci >= 1`
  - `terraform-validate >= 1`
  - `slo-reliability >= 1`

2. Release-integrity continuity:
- Kept latest + latest10 release verification and evidence contract checks in required workflows.
- Kept weekly evidence + rollback drill path in active operation.

3. Version alignment:
- Updated project/app/chart/Terraform defaults to `1.1.0-rc1` / `v1.1.0-rc1`.

## Compatibility

- HTTP endpoints unchanged:
  - `POST /v1/chat/completions`
  - `POST /v1/embeddings`
  - `GET /v1/models`
- No breaking contract change in `docs/contracts/v1/*`.

## Migration Notes from v1.1.0-alpha.2

- No migration required.
- Continue enforcing these gates before GA tag:
  - same-commit `release-verify` success
  - `check_ga_release_gate.py` pass

## Known Limitations

- Inflight/backpressure state remains in-process.
- Reliability signals are synthetic benchmark/fault/soak artifacts, not direct production telemetry.
