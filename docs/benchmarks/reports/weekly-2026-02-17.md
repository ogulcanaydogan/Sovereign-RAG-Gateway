# Weekly Benchmark and Quality Report (2026-02-17)

## Scope
- Runnable governance gateway baseline.
- Week-5 RAG foundation: connector registry, filesystem retrieval, citations, and ingestion tooling.

## Test Summary
- `ruff check .`: pass
- `mypy app scripts`: pass
- `pytest`: pass (`35 passed`)
- `scripts/validate_schemas.py`: pass
- `scripts/compose_smoke.sh`: pass

## Benchmark Snapshot
- Scenarios: `direct_provider`, `observe_mode`, `enforce_redact`
- Dataset: `benchmarks/data/synthetic_prompts.jsonl`
- Output path: `artifacts/benchmarks`
- Generated artifacts per scenario:
  - `raw/request_metrics.csv`
  - `results_summary.json`
  - `report.md`

## Retrieval Snapshot
- Connector support: `filesystem` (policy-constrained)
- Ingestion commands:
  - `python3 scripts/generate_synthetic_healthcare_corpus.py --output-dir benchmarks/data/synthetic_corpus`
  - `python3 scripts/rag_ingest.py --input-dir benchmarks/data/synthetic_corpus --output artifacts/rag/filesystem_index.jsonl`
- API behavior: RAG-enabled chat requests now include `choices[].message.citations[]` when chunks are retrieved.

## Known Limitations
- Retrieval is lexical for baseline; vector ranking and pgvector connector are pending.
- Policy integration is still local-stub-first unless remote OPA URL is configured.
- Benchmark harness emits synthetic metrics; live-load and fault-injection integration is pending.
