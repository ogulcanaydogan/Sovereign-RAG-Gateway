apiVersion: v1
kind: Service
metadata:
  name: {{ include "srg.fullname" . }}
  labels:
    {{- include "srg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "srg.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
